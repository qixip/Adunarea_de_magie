export const environment = {
  production: true,
  // If your backend lives on the same domain under /api/, leave this empty.
  // If it's on a separate server (e.g. a VPS or subdomain), set the full
  // origin here: 'https://api.yourdomain.com'
  apiBaseUrl: ''
};
